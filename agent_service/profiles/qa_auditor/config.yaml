# QA Auditor Profile Configuration
model:
  default: "google/gemini-2.5-flash"
  provider: "openrouter"

agent:
  reasoning_effort: "high"

toolsets:
  - "kanban"
  - "terminal"
  - "file_ops"

runtime:
  nofile_soft_limit: 4096
