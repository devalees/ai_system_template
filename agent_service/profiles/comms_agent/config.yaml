# Communications Coordinator Profile Configuration
model:
  default: "google/gemini-2.5-flash"
  provider: "openrouter"

agent:
  reasoning_effort: "low"

toolsets:
  - "file_ops"
  - "terminal"

runtime:
  nofile_soft_limit: 4096
