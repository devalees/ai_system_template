# Cost Controller Profile Configuration
model:
  default: "google/gemini-2.5-flash"
  provider: "openrouter"

agent:
  reasoning_effort: "low"

toolsets:
  - "terminal"
  - "file_ops"
  - "web"

runtime:
  nofile_soft_limit: 4096
